// exercise.js - Practice with objects

// TODO 1: Create an object 'person' with 'firstName', 'lastName', and 'age'.

// TODO 2: Add a method 'fullName' to the 'person' object that 
// returns the combination of first and last name.
// (Hint: use 'this')

// TODO 3: Use destructuring to extract 'age' from the 'person' object 
// into a constant.

// TODO 4: Use the spread operator (...) to create a new object 
// 'updatedPerson' that has the same info as 'person' but 'age' is 1 year older.

// TODO 5: Use bracket notation [] to access a property using a variable 'propName'.
// const propName = "firstName";
// const val = ...

// TODO 6: Use optional chaining (?.) and nullish coalescing (??) 
// to safely access 'person.address.city' (which doesn't exist) 
// and provide "Unknown" as a default.

// CHALLENGE: Create an object 'calculator' with 'add(a, b)' and 'sub(a, b)' methods.

// --- Verification ---
console.log("--- Results ---");
try {
    // console.log(`Full Name: ${person.fullName()}`);
    // console.log(`City: ${city}`);
} catch (e) {
    console.log("Error: fill in all TODOs to see results!");
}
