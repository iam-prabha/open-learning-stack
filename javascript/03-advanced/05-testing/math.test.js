// math.test.js - Conceptual Jest Syntax 
// Note: This requires Jest installed to run, 
// but we show the syntax for learning.

// const { sum, divide } = require("./math");

/*
describe("Math Suite", () => {

    test("adds 1 + 2 to equal 3", () => {
        expect(sum(1, 2)).toBe(3);
    });

    test("throws error when dividing by zero", () => {
        expect(() => divide(10, 0)).toThrow("Zero division");
    });

    test("result is close to 0.3 (floating point math)", () => {
        expect(sum(0.1, 0.2)).toBeCloseTo(0.3);
    });

});
*/

console.log("--- Testing Concepts ---");
console.log("1. describe(): Groups related tests.");
console.log("2. test/it(): A single unit of verification.");
console.log("3. expect(): The value you are checking.");
console.log("4. Matchers (.toBe, .toThrow): The rule for success.");
