# exercise.py - Practice with functions

# TODO 1: Create a function named 'square' that takes a number 'n' 
# as an argument and returns the square of that number (n * n).
# def square(n):
#     ...

# TODO 2: Create a function named 'is_even' that takes a number 'n' 
# and returns True if the number is even, and False otherwise.
# def is_even(n):
#     ...

# TODO 3: Create a function named 'get_full_name' that takes 'first_name' 
# and 'last_name' as arguments and returns a single string with both names 
# separated by a space.
# def get_full_name(first_name, last_name):
#     ...

# TODO 4: Create a function named 'calculate_tax' that takes 'price' and 
# an optional 'tax_rate' (default should be 0.05). 
# It should return the calculated tax amount (price * tax_rate).
# def calculate_tax(price, tax_rate=0.05):
#     ...

# TODO 5: Create a function named 'repeat_word' that takes a 'word' 
# and a 'count' and returns the word repeated 'count' times, separated by a space.
# (Hint: use range() and a list or a simple loop)
# def repeat_word(word, count):
#     ...

# CHALLENGE: 
# Create a function named 'apply_operation' that takes three arguments: 
# a function 'func', and two numbers 'a' and 'b'. 
# It should return the result of calling 'func(a, b)'.
# (This demonstrates that functions are first-class objects in Python!)
# ...

# --- Verification ---
print("--- Results ---")
# Use try-except to avoid stopping on incomplete TODOs
try:
    # Run tests here
    pass
except NameError as e:
    print(f"Error: {e} - Make sure you filled in all TODOs!")
